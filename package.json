{
  "name": "kagura-search",
  "version": "0.0.0",
  "private": true,
  "packageManager": "npm@11.11.0",
  "description": "Truth-illuminating open-source search engine",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "dev": "turbo dev"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
